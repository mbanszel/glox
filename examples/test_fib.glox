var start;
var end;
var duration;

fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 2) + fib(n - 1);
}

start = clock();

for (var i = 0; i < 30; i = i + 1) {
  print fib(i);
}
end = clock();

duration = end - start;
print "duration: ";
print duration;
